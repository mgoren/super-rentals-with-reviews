import { rentalPopularity } from 'super-rentals/helpers/rental-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | rental popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPopularity([42]);
  assert.ok(result);
});
